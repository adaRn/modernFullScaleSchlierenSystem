Grid made for printing
===

Spacing is a bit below 5mm. Accuracy doesn't matter here, as long as its `black` and `white` stripes have exactly the same width.

`hotWater` test was made with this grid.
